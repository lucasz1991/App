# Container OS UID54321/postgres can do explicit cluster administration locally.
local   all       postgres                           peer
local   all       all                                reject
# Runtime role is provisioned explicitly, never by compose up or initdb.
host    openuem   openuem    0.0.0.0/0                scram-sha-256
host    openuem   openuem    ::/0                     scram-sha-256
host    all       all        0.0.0.0/0                reject
host    all       all        ::/0                     reject
